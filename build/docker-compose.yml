services:
  rocky10build:
    build:
      context: .
      args:
        centos: $RL10
    image: "aursu/php83build:${RL10TAG}-build"
  rocky9build:
    build:
      context: .
      args:
        centos: $RL9
    image: "aursu/php83build:${RL9TAG}-build"
